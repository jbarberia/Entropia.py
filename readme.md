# Entropia.py

Este paquete centraliza diferentes tipos de entropias de la información.

# Descarga e instalacion


Sencillamente se descarga el repositorio con Git y se instala.
```
git clone URL
pip install -r requirements.txt
python setup.py install
```
Tambien puede utilizarse pip
```
pip install cython
pip install git+url
```
